{
  "type": "object",
  "title": "extractor configuration",
  "required": [
    "client_id",
    "#client_secret",
    "tenant_id",
    "incremental"
  ],
  "properties": {
    "client_id": {
      "type": "string",
      "title": "Client ID",
      "propertyOrder": 10,
      "description": "Azure Application (client) ID"
    },

    "#client_secret": {
      "type": "string",
      "title": "Client Secret",
      "propertyOrder": 20,
      "description": "Azure App Registration secret VALUE"
    },

    "tenant_id": {
      "type": "string",
      "title": "Tenant ID",
      "propertyOrder": 30,
      "description": "Azure Tenant ID"
    },

    "incremental": {
      "type": "boolean",
      "title": "Incremental",
      "default": false,
      "format": "checkbox",
      "propertyOrder": 40
    }
  }
}